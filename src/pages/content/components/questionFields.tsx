import React from "react";

type Props = {
  newApplication: Application;
  updateNewApplication: (application: Application) => void;
};

export const QuestionFields = ({
  newApplication,
  updateNewApplication,
}: Props) => {
  return <div>questionFields</div>;
};
